# Future Work

Follow-on items this ticket enables but does not include. Use **bold leads** to name each item (e.g., "Migration ticket", "Cleanup", "Consolidation").

If Future Work items overlap with Out Of Scope items, drop Future Work -- the two sections shouldn't duplicate each other.
